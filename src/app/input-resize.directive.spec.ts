import { InputResizeDirective } from './input-resize.directive';

describe('InputResizeDirective', () => {
  it('should create an instance', () => {
    const directive = new InputResizeDirective();
    expect(directive).toBeTruthy();
  });
});
